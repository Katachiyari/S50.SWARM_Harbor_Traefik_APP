http:
  routers:
    harbor:
      rule: "Host(`harbor.app.local`)"
      entryPoints:
        - websecure
      service: harbor
      tls: {}

  services:
    harbor:
      loadBalancer:
        servers:
          - url: "http://192.168.56.10"
