http:
  routers:
    app:
      rule: "Host(`{{ app_domain }}`)"
      service: app
      entryPoints:
        - web
  
  services:
    app:
      loadBalancer:
        servers:
          - url: "http://{{ app_service_name }}:{{ app_port }}"
